document.getElementsByClassName('back')[0].addEventListener('click', back);
document.getElementsByClassName('back')[0].addEventListener('touchstart', back);

function back(){
    window.history.back();
}

